import 'package:flutter/material.dart';

class AdminPanelStubScreen extends StatelessWidget {
  // Конструктор теперь const
  const AdminPanelStubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Admin Panel is not available in this build.'),
      ),
    );
  }
}